export interface IcreatedFolders {
    path: string
    created: boolean
}

export interface IjsonCantores {
    sopranos: string[]
    mezzoSopranos: string[]
    contraltos: string[]
    baixos: string[]
    barítonos: stirng[]
    tenores: string[]
}